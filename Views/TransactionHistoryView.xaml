<Window x:Class="InvestingManagerApp.Views.TransactionHistoryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="История транзакций" Height="500" Width="850">

    <DockPanel>
        <!-- Меню -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Навигация">
                <MenuItem Header="Портфели" Click="NavigateToPortfolios_Click"/>
                <MenuItem Header="История транзакций" IsEnabled="False"/>
            </MenuItem>
        </Menu>

        <!-- Основное содержимое -->
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="История транзакций" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>

            <DataGrid Grid.Row="1" 
                      ItemsSource="{Binding Transactions}" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      Margin="0,10,0,10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                    <DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="Auto"/>
                    <DataGridTextColumn Header="Тикер" Binding="{Binding SecurityTicker}" Width="*"/>
                    <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=\{0:dd.MM.yyyy\}}" Width="Auto"/>
                    <DataGridTextColumn Header="Цена/ед." Binding="{Binding PricePerUnit}" Width="Auto"/>
                    <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="Auto"/>
                    <DataGridTextColumn Header="Выплата/ед." Binding="{Binding PaymentPerUnit}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>

            <Button Grid.Row="2" Content="Назад" 
                    Width="100" Height="30" 
                    HorizontalAlignment="Left"
                    Click="BackButton_Click"/>
        </Grid>
    </DockPanel>
</Window>