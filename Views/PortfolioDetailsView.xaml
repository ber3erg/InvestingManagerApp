<Window x:Class="InvestingManagerApp.Views.PortfolioDetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ценные бумаги портфеля" Height="500" Width="850">
    <DockPanel>
        <!-- Меню -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Навигация">
                <MenuItem Header="Портфели" Click="NavigateToPortfolioList_Click"/>
                <MenuItem Header="История транзакций" Click="NavigateToTransactionHistory_Click"/>
            </MenuItem>
        </Menu>

        <!-- Основное содержимое -->
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Ценные бумаги портфеля" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>

            <DataGrid Grid.Row="1" 
                      ItemsSource="{Binding Securities}" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      Margin="0,10,0,10"
                      x:Name="DataGrid">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Тикер" Binding="{Binding Ticker}" Width="Auto"/>
                    <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="Auto"/>
                    <DataGridTextColumn Header="Текущая стоимость" Binding="{Binding CurrentPrice, StringFormat=\{0:C\}}" Width="Auto"/>
                    <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>

            <Button Grid.Row="2" Content="Редактировать ценную бумагу" 
                    Width="150" Height="30" 
                    HorizontalAlignment="Left"
                    Click="EditSecurityButton_Click"/>

            <Button Grid.Row="2" Content="Назад" 
                    Width="100" Height="30" 
                    HorizontalAlignment="Right"
                    Click="BackButton_Click"/>
        </Grid>
    </DockPanel>
</Window>